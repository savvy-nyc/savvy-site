import Default from "../layouts/default";

class HomePage extends Component {
  render() {
    return (
      <Default>
        <h1>Homepage</h1>
      </Default>
    );
  }
}

export default HomePage;
